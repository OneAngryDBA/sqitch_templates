-- ========================================================================== --
-- Deploy [% change %]
[% FOREACH item IN requires -%]
-- requires: [% item %]
[% END -%]
[% FOREACH item IN conflicts -%]
-- conflicts: [% item %]
[% END -%]
-- ========================================================================== --
BEGIN;

SET client_min_messages = 'warning';

-- XXX Add DDLs here.

COMMIT;
